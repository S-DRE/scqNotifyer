<% layout('layout') -%>
<% script("../index.js")%>
<div class="container-fluid">
    <h1 class="mt-4"></h1>
    <div class="row">
        <div class="col-xl-6">
            <div class="card text-center">
                <div class="card-header">
                    <i class="fas fa-info-circle fa-2x"></i><strong> SCQ Notifyer</strong>
                </div>
                <div class="card-body">
                    <p class="card-text">Bienvenido/a <%if (cliente !== null) { %>
                        <%-cliente.first_name+" "+cliente.last_name%>
                        <% } else { %>
                        a SCQNotifyer, por favor, inicie sesión.
                        <% } %></p>
                    <p class="card-text">Retaíla de info</p>
                </div>
            </div>
        </div>
    </div>
</div>